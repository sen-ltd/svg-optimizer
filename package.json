{
  "name": "svg-optimizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client-side SVG optimizer with configurable transformations.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080"
  },
  "license": "MIT"
}
